<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      下記<%= pluralize(object.errors.count, "箇所") %>を確認シテください.
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>